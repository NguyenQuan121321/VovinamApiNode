meta {
  name: proposal list (admin)
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/v1/promotion-proposals?status=PENDING
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}
