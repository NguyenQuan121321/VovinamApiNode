meta {
  name: leave list is role-scoped
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/api/v1/leave-requests
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{instructorToken}}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}
