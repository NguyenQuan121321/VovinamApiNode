meta {
  name: leave list filter by status
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/api/v1/leave-requests?status=PENDING
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}
