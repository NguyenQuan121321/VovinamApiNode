meta {
  name: admin deletes a leave request
  type: http
  seq: 13
}

delete {
  url: {{baseUrl}}/api/v1/leave-requests/{{leaveParentId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}
