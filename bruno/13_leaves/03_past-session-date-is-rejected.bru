meta {
  name: past session date is rejected
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/leave-requests
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{studentToken}}
  content-type: application/json
}

body:json {
  {"studentId": "{{studentId}}", "classId": "{{basicClassId}}", "sessionDate": "2026-09-24", "reason": "too late"}
}

tests {
  test("status 400", function() { expect(res.status).to.equal(400); });
}
