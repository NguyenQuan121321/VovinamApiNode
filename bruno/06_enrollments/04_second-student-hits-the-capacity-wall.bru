meta {
  name: second student hits the capacity wall
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/enrollments
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
  content-type: application/json
}

body:json {
  {"studentId": "{{newStudentId}}", "classId": "{{smallClassId}}"}
}

tests {
  test("status 409", function() { expect(res.status).to.equal(409); });
}
