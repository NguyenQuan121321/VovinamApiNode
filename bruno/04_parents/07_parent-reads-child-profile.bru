meta {
  name: parent reads child profile
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/students/{{minorBId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{parentToken}}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}
