meta {
  name: readyz readiness
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/readyz
  body: none
  auth: none
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
  test("database is up", function() { expect(res.body.data.database).to.equal("up"); });
}
