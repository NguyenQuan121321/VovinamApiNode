meta {
  name: duplicate order index conflicts
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/belt-ranks
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
  content-type: application/json
}

body:json {
  {"code": "UAT_X2", "name": "Dup", "rankGroup": "DO", "orderIndex": 999}
}

tests {
  test("status 409", function() { expect(res.status).to.equal(409); });
}
