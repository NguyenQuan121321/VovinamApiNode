meta {
  name: student cannot create ranks
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/belt-ranks
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{studentToken}}
  content-type: application/json
}

body:json {
  {"code": "HACK", "name": "H", "rankGroup": "DO", "orderIndex": 5}
}

tests {
  test("status 403", function() { expect(res.status).to.equal(403); });
}
