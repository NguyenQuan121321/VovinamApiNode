meta {
  name: list users as student is forbidden
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/users
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{studentToken}}
}

tests {
  test("status 403", function() { expect(res.status).to.equal(403); });
}
