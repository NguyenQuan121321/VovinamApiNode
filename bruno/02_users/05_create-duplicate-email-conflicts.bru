meta {
  name: create duplicate email conflicts
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/users
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
  content-type: application/json
}

body:json {
  {"email": "uat-instructor2@example.com", "password": "UatInstr#2026", "role": "INSTRUCTOR"}
}

tests {
  test("status 409", function() { expect(res.status).to.equal(409); });
}
