meta {
  name: logout-all for the parent
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/logout-all
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{parentToken}}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}
