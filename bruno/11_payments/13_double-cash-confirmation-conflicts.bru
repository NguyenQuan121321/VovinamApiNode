meta {
  name: double cash confirmation conflicts
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/api/v1/payments/{{manualInvoiceId}}/confirm-cash
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
  content-type: application/json
}

body:json {
  {}
}

tests {
  test("status 409", function() { expect(res.status).to.equal(409); });
}
