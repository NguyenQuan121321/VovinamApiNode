meta {
  name: invoice became PAID
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/invoices/{{tuitionInvoiceId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{studentToken}}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
  test("status PAID", function() { expect(res.body.data.status).to.equal("PAID"); });
}
