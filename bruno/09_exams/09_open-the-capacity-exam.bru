meta {
  name: open the capacity exam
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/api/v1/belt-exams/{{fullExamId}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
  content-type: application/json
}

body:json {
  {"status": "OPEN"}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}
