meta {
  name: open the FAIL exam
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/api/v1/belt-exams/{{failExamId}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
  content-type: application/json
}

body:json {
  {"status": "OPEN"}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}
