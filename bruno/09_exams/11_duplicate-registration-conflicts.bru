meta {
  name: duplicate registration conflicts
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/api/v1/belt-exams/{{passExamId}}/register
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{studentToken}}
  content-type: application/json
}

body:json {
  {"studentId": "{{studentId}}"}
}

tests {
  test("status 409", function() { expect(res.status).to.equal(409); });
}
