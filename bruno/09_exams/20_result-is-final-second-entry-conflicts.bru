meta {
  name: result is final (second entry conflicts)
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/api/v1/exam-registrations/{{passRegId}}/result
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{instructorToken}}
  content-type: application/json
}

body:json {
  {"status": "RESULT_FAIL"}
}

tests {
  test("status 409", function() { expect(res.status).to.equal(409); });
}
