meta {
  name: student directory is forbidden
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/students
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{studentToken}}
}

tests {
  test("status 403", function() { expect(res.status).to.equal(403); });
}
