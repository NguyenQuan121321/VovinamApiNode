import { Injectable } from '@nestjs/common';

/** Placeholder replaced by the full auth flows in phase P1 (plan section 5). */
@Injectable()
export class AuthService {
  readonly implementedInPhase = 'P1';
}
