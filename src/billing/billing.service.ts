import { Injectable } from '@nestjs/common';

/** Placeholder replaced by invoices, payments and reports in phase P4 (plan section 7.5). */
@Injectable()
export class BillingService {
  readonly implementedInPhase = 'P4';
}
