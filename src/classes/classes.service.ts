import { Injectable } from '@nestjs/common';

/** Placeholder replaced by classes, schedules and enrollments in phase P2 (plan section 8). */
@Injectable()
export class ClassesService {
  readonly implementedInPhase = 'P2';
}
